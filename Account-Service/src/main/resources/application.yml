server:
  port: 9092

spring:
  application:
    name: Account-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/bank_service
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true