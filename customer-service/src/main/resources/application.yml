server:
  port: 9091

spring:
  application:
    name: CUSTOMER-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/bank_service
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true